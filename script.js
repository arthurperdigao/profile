const translations = {
    pt: {
        nav_home: "Home",
        nav_projects: "Projetos",
        nav_stack: "Stack",
        hero_subtitle: "Especialista na construção de sistemas robustos e sites web de alta performance.",
        cta_projects: "Ver Projetos",
        cta_contact: "Contato",
        projects_title: "Projetos em Destaque",
        project_flagship_title: "Bender — Portal Avatar",
        project_flagship_desc: "O Cargo Chefe: Uma comunidade completa com interações sociais, banco de dados robusto, mini-games autorais e sistema de Bazaar (loja) integrado.",
        project2_desc: "Sistema de reserva de viagens e galeria interativa para a Agência Santa Edwirges.",
        apiflow_desc: "Ferramenta visual de drag-and-drop para testes de integração de API.",
        arenafacil_title: "Arena Fácil — SaaS Management",
        arenafacil_desc: "Sistema completo de reservas de quadras, controle de comandas em tempo real e BI.",
        arenafacil_img1: "Dashboard Principal: Visão geral em tempo real de faturamento e agendamentos.",
        arenafacil_img2: "Controle de Acesso: Gestão de usuários, níveis de permissão e segurança.",
        arenafacil_img3: "Sistema de Agendamento: Interface interativa para reservas de espaços esportivos.",
        arenafacil_img4: "Comandas em Tempo Real: Controle dinâmico de loja e consumo interno.",
        arenafacil_img5: "Business Intelligence: Relatórios financeiros detalhados e análise de métricas.",
        cta_visit: "Visitar Projeto",
        stack_title: "Tech Stack",
        footer_rights: "Todos os direitos reservados.",
        projects: {
            bender: {
                title: "Bender — Portal Avatar",
                desc: "Uma comunidade completa com sistema de lojas, mini-games e interações sociais em tempo real.",
                link: "https://bender-goll.vercel.app/",
                gallery: [
                    { url: "C:\\Users\\Arthur Perdigao\\.gemini\\antigravity\\brain\\5b45f7dd-2018-45c1-bf35-ebbf39e52039\\media__1778100841550.png", caption: "Home — Legado Elemental" },
                    { url: "C:\\Users\\Arthur Perdigao\\.gemini\\antigravity\\brain\\5b45f7dd-2018-45c1-bf35-ebbf39e52039\\media__1778101088928.png", caption: "Biblioteca de Pergaminhos — Sistema de Leitura de HQs" },
                    { url: "C:\\Users\\Arthur Perdigao\\.gemini\\antigravity\\brain\\5b45f7dd-2018-45c1-bf35-ebbf39e52039\\media__1778101130805.png", caption: "Jornada do Conhecimento — Mini-games e Quizzes" },
                    { url: "C:\\Users\\Arthur Perdigao\\.gemini\\antigravity\\brain\\5b45f7dd-2018-45c1-bf35-ebbf39e52039\\media__1778101159649.png", caption: "Bazar de Omashu — Sistema de Loja e Itens" }
                ]
            },
            caravana: {
                title: "Caravana da Vera — Santa Edwirges",
                desc: "Sistema completo de reservas de viagens, gestão de destinos e galeria de momentos interativa.",
                link: "https://caravanadavera.com.br/",
                gallery: [
                    { url: "C:\\Users\\Arthur Perdigao\\.gemini\\antigravity\\brain\\5b45f7dd-2018-45c1-bf35-ebbf39e52039\\media__1778101892449.png", caption: "Landing Page — Jornada Religiosa" },
                    { url: "C:\\Users\\Arthur Perdigao\\.gemini\\antigravity\\brain\\5b45f7dd-2018-45c1-bf35-ebbf39e52039\\media__1778101925090.png", caption: "Pacotes de Viagem — Destinos Disponíveis" },
                    { url: "C:\\Users\\Arthur Perdigao\\.gemini\\antigravity\\brain\\5b45f7dd-2018-45c1-bf35-ebbf39e52039\\media__1778101946507.png", caption: "Galeria de Momentos — Registros dos Passageiros" }
                ]
            },
            apiflow: {
                title: "APIFlowTester",
                desc: "Ferramenta visual inovadora para simplificar testes de integração complexos através de um canvas drag-and-drop.",
                link: "https://github.com/arthurperdigao/APIFlowTester",
                gallery: [
                    { url: "C:\\Users\\Arthur Perdigao\\.gemini\\antigravity\\brain\\5b45f7dd-2018-45c1-bf35-ebbf39e52039\\media__1778101776373.png", caption: "Canvas Interativo e Resultados de Execução" }
                ]
            },
            arenafacil: {
                title: "Arena Fácil — SaaS Management",
                desc: "Sistema completo de reservas de quadras, controle de comandas em tempo real e BI.",
                link: "#",
                gallery: [
                    { url: "arena%20facil/dash%20board.png", caption: "Dashboard Principal: Visão geral em tempo real de faturamento e agendamentos." },
                    { url: "arena%20facil/hierarquia%20de%20acesso%20ao%20sistema.png", caption: "Controle de Acesso: Gestão de usuários, níveis de permissão e segurança." },
                    { url: "arena%20facil/agendamento%20de%20quadra.png", caption: "Sistema de Agendamento: Interface interativa para reservas de espaços esportivos." },
                    { url: "arena%20facil/Sistema%20de%20controle%20de%20comanda.png", caption: "Comandas em Tempo Real: Controle dinâmico de loja e consumo interno." },
                    { url: "arena%20facil/relatorio%20branco.png", caption: "Business Intelligence: Relatórios financeiros detalhados e análise de métricas." }
                ]
            }
        }
    },
    en: {
        nav_home: "Home",
        nav_projects: "Projects",
        nav_stack: "Stack",
        hero_subtitle: "Specialist in engineering robust systems and high-performance web applications.",
        cta_projects: "View Projects",
        cta_contact: "Contact",
        projects_title: "Featured Projects",
        project_flagship_title: "Bender — Avatar Portal",
        project_flagship_desc: "Flagship Project: A comprehensive community platform with social interactions, robust database systems, custom mini-games, and an integrated Bazaar (shop).",
        project2_desc: "Travel booking system and interactive gallery for Santa Edwirges Agency.",
        apiflow_desc: "Visual drag-and-drop tool for API integration testing.",
        arenafacil_title: "Arena Fácil — SaaS Management",
        arenafacil_desc: "Complete system for court reservations, real-time command control and business BI.",
        arenafacil_img1: "Main Dashboard: Real-time overview of business performance and active bookings.",
        arenafacil_img2: "Access Control: Management of users, permissions and administrative security.",
        arenafacil_img3: "Scheduling System: Interactive calendar for sports facility management.",
        arenafacil_img4: "Real-time Commands: Dynamic store and point-of-sale control for arenas.",
        arenafacil_img5: "Business Intelligence: Detailed financial reports and usage analytics.",
        cta_visit: "Visit Project",
        stack_title: "Tech Stack",
        footer_rights: "All rights reserved.",
        projects: {
            bender: {
                title: "Bender — Avatar Portal",
                desc: "A comprehensive community platform with shops, mini-games, and real-time social interactions.",
                link: "https://bender-goll.vercel.app/",
                gallery: [
                    { url: "bender%20avatar/home%20principal.png", caption: "Home — Elemental Legacy" },
                    { url: "bender%20avatar/biblioteca%20de%20livros.png", caption: "Scroll Library — Comic Reader System" },
                    { url: "bender%20avatar/loja%20mundo%20avatar.png", caption: "Omashu Bazaar — Shop and Item System" }
                ]
            },
            caravana: {
                title: "Caravana da Vera — Santa Edwirges",
                desc: "Complete travel booking system, destination management, and interactive moments gallery.",
                link: "https://caravanadavera.com.br/",
                gallery: [
                    { url: "caravana%20da%20vera/home%20principal.png", caption: "Landing Page — Religious Journey" },
                    { url: "caravana%20da%20vera/pacotes%20de%20viagem.png", caption: "Travel Packages — Available Destinations" },
                    { url: "caravana%20da%20vera/momentos%20da%20viagem.png", caption: "Moments Gallery — Passenger Records" }
                ]
            },
            apiflow: {
                title: "APIFlowTester",
                desc: "Innovative visual tool designed to simplify complex integration tests through a drag-and-drop canvas.",
                link: "https://github.com/arthurperdigao/APIFlowTester",
                gallery: [
                    { url: "apitesteflow/456168149-1108bcc0-35a7-438c-9d58-081b73e78ea0.png", caption: "Interactive Canvas and Execution Results" }
                ]
            },
            arenafacil: {
                title: "Arena Fácil — SaaS Management",
                desc: "Complete system for court reservations, real-time command control and business BI.",
                link: "#",
                gallery: [
                    { url: "arena%20facil/dash%20board.png", caption: "Main Dashboard: Real-time overview of business performance and active bookings." },
                    { url: "arena%20facil/hierarquia%20de%20acesso%20ao%20sistema.png", caption: "Access Control: Management of users, permissions and administrative security." },
                    { url: "arena%20facil/agendamento%20de%20quadra.png", caption: "Scheduling System: Interactive calendar for sports facility management." },
                    { url: "arena%20facil/Sistema%20de%20controle%20de%20comanda.png", caption: "Real-time Commands: Dynamic store and point-of-sale control for arenas." },
                    { url: "arena%20facil/relatorio%20branco.png", caption: "Business Intelligence: Detailed financial reports and usage analytics." }
                ]
            }
        }
    }
};

const flags = {
    pt: "🇧🇷",
    en: "🇺🇸"
};

let currentLang = 'pt';
let openProjectId = null; // Track which project is open in the modal

function updateContent(lang) {
    const elements = document.querySelectorAll('[data-i18n]');
    const app = document.getElementById('app');
    
    // Smooth transition effect
    app.style.opacity = '0';
    
    setTimeout(() => {
        elements.forEach(el => {
            const key = el.getAttribute('data-i18n');
            if (translations[lang][key]) {
                el.textContent = translations[lang][key];
            }
        });

        // Update Modal if open
        if (openProjectId) {
            updateModalData(openProjectId);
        }

        // Update selector UI
        document.getElementById('current-flag').textContent = flags[lang];
        document.getElementById('current-lang').textContent = lang.toUpperCase();
        
        // Update document lang
        document.documentElement.lang = lang === 'pt' ? 'pt-BR' : 'en-US';
        
        app.style.opacity = '1';
    }, 300);
}

function setLanguage(lang) {
    console.log('Setting language to:', lang);
    if (!translations[lang]) {
        console.error('Translation not found for:', lang);
        return;
    }
    currentLang = lang;
    localStorage.setItem('preferred-lang', lang);
    updateContent(lang);
}

// Language Detection
function detectLanguage() {
    const saved = localStorage.getItem('preferred-lang');
    if (saved && translations[saved]) return saved;
    
    const browserLang = navigator.language.split('-')[0];
    return (translations[browserLang]) ? browserLang : 'en';
}

// Initial Setup
document.addEventListener('DOMContentLoaded', () => {
    const initialLang = detectLanguage();
    setLanguage(initialLang);

    // Event Listeners for options
    document.querySelectorAll('.lang-option').forEach(option => {
        option.addEventListener('click', (e) => {
            e.preventDefault();
            e.stopPropagation();
            const selectedLang = option.getAttribute('data-lang');
            console.log('Option clicked:', selectedLang);
            setLanguage(selectedLang);
        });
    });

    // Custom Cursor Logic
    const cursorDot = document.querySelector(".cursor-dot");
    const cursorOutline = document.querySelector(".cursor-outline");

    window.addEventListener("mousemove", (e) => {
        const posX = e.clientX;
        const posY = e.clientY;

        cursorDot.style.left = `${posX}px`;
        cursorDot.style.top = `${posY}px`;

        // Smooth outline follow
        cursorOutline.animate({
            left: `${posX}px`,
            top: `${posY}px`
        }, { duration: 500, fill: "forwards" });
    });

    // Cursor hover effects
    const hoverElements = document.querySelectorAll("a, button, .project-card, .stack-item");
    hoverElements.forEach(el => {
        el.addEventListener("mouseenter", () => {
            cursorOutline.classList.add("cursor-hover");
            cursorDot.style.transform = "translate(-50%, -50%) scale(0.5)";
        });
        el.addEventListener("mouseleave", () => {
            cursorOutline.classList.remove("cursor-hover");
            cursorDot.style.transform = "translate(-50%, -50%) scale(1)";
        });
    });

    // Scroll Reveal Logic
    const revealCallback = (entries) => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                entry.target.classList.add("active");
            }
        });
    };

    const revealObserver = new IntersectionObserver(revealCallback, {
        threshold: 0.15
    });

    document.querySelectorAll(".reveal").forEach(el => {
        revealObserver.observe(el);
    });

    // Magnetic Effect
    const magneticElements = document.querySelectorAll('.magnetic');
    magneticElements.forEach(el => {
        el.addEventListener('mousemove', (e) => {
            const rect = el.getBoundingClientRect();
            const x = e.clientX - rect.left - rect.width / 2;
            const y = e.clientY - rect.top - rect.height / 2;
            el.style.transform = `translate(${x * 0.3}px, ${y * 0.3}px)`;
        });
        el.addEventListener('mouseleave', () => {
            el.style.transform = `translate(0px, 0px)`;
        });
    });

    // Text Scramble Effect
    class TextScramble {
        constructor(el) {
            this.el = el;
            this.chars = '!<>-_\\/[]{}—=+*^?#________';
            this.update = this.update.bind(this);
        }
        setText(newText) {
            const oldText = this.el.innerText;
            const length = Math.max(oldText.length, newText.length);
            const promise = new Promise((resolve) => this.resolve = resolve);
            this.queue = [];
            for (let i = 0; i < length; i++) {
                const from = oldText[i] || '';
                const to = newText[i] || '';
                const start = Math.floor(Math.random() * 40);
                const end = start + Math.floor(Math.random() * 40);
                this.queue.push({ from, to, start, end });
            }
            cancelAnimationFrame(this.frameRequest);
            this.frame = 0;
            this.update();
            return promise;
        }
        update() {
            let output = '';
            let complete = 0;
            for (let i = 0, n = this.queue.length; i < n; i++) {
                let { from, to, start, end, char } = this.queue[i];
                if (this.frame >= end) {
                    complete++;
                    output += to;
                } else if (this.frame >= start) {
                    if (!char || Math.random() < 0.28) {
                        char = this.randomChar();
                        this.queue[i].char = char;
                    }
                    output += `<span class="dud">${char}</span>`;
                } else {
                    output += from;
                }
            }
            this.el.innerHTML = output;
            if (complete === this.queue.length) {
                this.resolve();
            } else {
                this.frameRequest = requestAnimationFrame(this.update);
                this.frame++;
            }
        }
        randomChar() {
            return this.chars[Math.floor(Math.random() * this.chars.length)];
        }
    }

    const el = document.querySelector('.scramble');
    const fx = new TextScramble(el);
    const originalText = el.innerText;
    
    // Scramble on load and hover
    setTimeout(() => fx.setText(originalText), 1000);
    el.addEventListener('mouseenter', () => fx.setText(originalText));

    // X-Ray Mode Logic
    const xrayToggle = document.getElementById('xray-toggle');
    xrayToggle.addEventListener('click', () => {
        document.body.classList.toggle('xray-mode');
        if (document.body.classList.contains('xray-mode')) {
            console.log("X-Ray Mode Active: Bounding boxes visible.");
        }
    });

    // HUD Logic
    const hudRes = document.getElementById('hud-res');
    const hudScr = document.getElementById('hud-scr');
    const hudFps = document.getElementById('hud-fps');
    const hudLng = document.getElementById('hud-lng');

    let lastTime = performance.now();
    let frameCount = 0;

    function updateHUD() {
        // Update Resolution
        hudRes.innerText = `${window.innerWidth}x${window.innerHeight}`;

        // Update Scroll
        const scrollPercent = Math.round((window.scrollY / (document.documentElement.scrollHeight - window.innerHeight)) * 100);
        hudScr.innerText = `${scrollPercent}%`;

        // Update FPS
        frameCount++;
        const now = performance.now();
        if (now - lastTime >= 1000) {
            hudFps.innerText = frameCount;
            frameCount = 0;
            lastTime = now;
        }

        hudLng.innerText = currentLang.toUpperCase();
        requestAnimationFrame(updateHUD);
    }
    updateHUD();

    // Constellation Canvas Logic
    const canvas = document.getElementById('bg-canvas');
    const ctx = canvas.getContext('2d');
    let particles = [];
    const particleCount = 120; // Dobrando a quantidade
    let mouse = { x: null, y: null, radius: 150 };

    function resize() {
        canvas.width = window.innerWidth;
        canvas.height = window.innerHeight;
    }

    window.addEventListener('resize', resize);
    resize();

    class Particle {
        constructor() {
            this.x = Math.random() * canvas.width;
            this.y = Math.random() * canvas.height;
            this.vx = (Math.random() - 0.5) * 0.5;
            this.vy = (Math.random() - 0.5) * 0.5;
            this.radius = Math.random() * 1.5;
        }
        update() {
            // Reação ao Mouse (Fuga)
            if (mouse.x != null) {
                let dx = mouse.x - this.x;
                let dy = mouse.y - this.y;
                let dist = Math.sqrt(dx * dx + dy * dy);
                if (dist < mouse.radius) {
                    let forceX = dx / dist;
                    let forceY = dy / dist;
                    this.x -= forceX * 2;
                    this.y -= forceY * 2;
                }
            }

            this.x += this.vx;
            this.y += this.vy;
            if (this.x < 0 || this.x > canvas.width) this.vx *= -1;
            if (this.y < 0 || this.y > canvas.height) this.vy *= -1;
        }
        draw() {
            ctx.beginPath();
            ctx.arc(this.x, this.y, this.radius, 0, Math.PI * 2);
            ctx.fillStyle = 'rgba(255, 255, 255, 0.6)';
            ctx.fill();
        }
    }

    for (let i = 0; i < particleCount; i++) particles.push(new Particle());

    function animateParticles() {
        ctx.clearRect(0, 0, canvas.width, canvas.height);
        particles.forEach((p, i) => {
            p.update();
            p.draw();
            for (let j = i + 1; j < particles.length; j++) {
                const p2 = particles[j];
                const dx = p.x - p2.x;
                const dy = p.y - p2.y;
                const dist = Math.sqrt(dx * dx + dy * dy);
                if (dist < 150) {
                    ctx.beginPath();
                    ctx.strokeStyle = `rgba(0, 112, 243, ${(1 - dist / 150) * 0.8})`;
                    ctx.lineWidth = 0.8;
                    ctx.moveTo(p.x, p.y);
                    ctx.lineTo(p2.x, p2.y);
                    ctx.stroke();
                }
            }
        });
        requestAnimationFrame(animateParticles);
    }
    animateParticles();

    window.addEventListener('mousemove', (e) => {
        mouse.x = e.clientX;
        mouse.y = e.clientY;
    });

    window.addEventListener('mouseout', () => {
        mouse.x = null;
        mouse.y = null;
    });

    // Project Modal Logic
    const modal = document.getElementById('project-modal');
    const modalImg = document.getElementById('modal-img');
    const modalCaption = document.getElementById('modal-caption');
    const modalTitle = document.getElementById('modal-title');
    const modalDesc = document.getElementById('modal-desc');
    const modalLink = document.getElementById('modal-link');
    const closeBtn = document.querySelector('.close-modal');
    
    let currentProjectGallery = [];
    let currentImgIndex = 0;

    document.querySelectorAll('.project-card').forEach(card => {
        card.addEventListener('click', () => {
            const projectId = card.getAttribute('data-project');
            openProjectId = projectId;
            updateModalData(projectId);
            
            modal.classList.add('active');
            modal.style.display = 'flex';
        });
    });

    function updateModalData(projectId) {
        const data = translations[currentLang].projects[projectId];
        if (!data) return;

        currentProjectGallery = data.gallery;
        modalTitle.innerText = data.title;
        modalDesc.innerText = data.desc;
        modalLink.href = data.link;
        updateModalImage();
    }

    function updateModalImage() {
        if (currentProjectGallery.length === 0) return;
        const item = currentProjectGallery[currentImgIndex % currentProjectGallery.length];
        modalImg.src = item.url;
        modalCaption.innerText = item.caption;
    }

    closeBtn.addEventListener('click', () => {
        modal.classList.remove('active');
        openProjectId = null;
        setTimeout(() => modal.style.display = 'none', 300);
    });

    window.addEventListener('click', (e) => {
        if (e.target === modal) {
            modal.classList.remove('active');
            openProjectId = null;
            setTimeout(() => modal.style.display = 'none', 300);
        }
    });

    // Gallery Nav
    document.querySelector('.gallery-nav.next').addEventListener('click', (e) => {
        e.stopPropagation();
        currentImgIndex = (currentImgIndex + 1) % currentProjectGallery.length;
        updateModalImage();
    });

    document.querySelector('.gallery-nav.prev').addEventListener('click', (e) => {
        e.stopPropagation();
        currentImgIndex = (currentImgIndex - 1 + currentProjectGallery.length) % currentProjectGallery.length;
        updateModalImage();
    });
    document.addEventListener('mousemove', (e) => {
        const parallaxElements = document.querySelectorAll('.parallax');
        const abstractShapes = document.querySelectorAll('.abstract-shape');
        
        const mouseX = e.clientX;
        const mouseY = e.clientY;
        
        parallaxElements.forEach(el => {
            const speed = el.getAttribute('data-speed') || 5; // Aumentando velocidade
            const x = (window.innerWidth - mouseX * speed) / 80;
            const y = (window.innerHeight - mouseY * speed) / 80;
            el.style.transform = `translateX(${x}px) translateY(${y}px)`;
        });

        abstractShapes.forEach((shape, index) => {
            const speed = (index + 1) * 5; // Movimento mais forte
            const x = (window.innerWidth - mouseX * speed) / 100;
            const y = (window.innerHeight - mouseY * speed) / 100;
            shape.style.transform = `translateX(${x}px) translateY(${y}px) rotate(${mouseX / 5}deg)`;
        });
    });
});
